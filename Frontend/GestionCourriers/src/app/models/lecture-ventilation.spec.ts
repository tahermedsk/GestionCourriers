import { LectureVentilation } from './lecture-ventilation';

describe('LectureVentilation', () => {
  it('should create an instance', () => {
    expect(new LectureVentilation()).toBeTruthy();
  });
});
